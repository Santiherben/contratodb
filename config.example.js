// Copy this file to `config.js` (in the project root) and fill the
// values. DO NOT commit `config.js` — it's included in .gitignore.

// Example runtime configuration that sets globals the app reads:
window.SUPABASE_URL = "https://your-project.supabase.co";
window.SUPABASE_ANON_KEY = "sb_publishable_yourPublicAnonKeyHere";

// Save as `config.js` and host it alongside your static files or set
// the same globals via your hosting provider's environment injection.